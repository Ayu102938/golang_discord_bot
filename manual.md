# Discord Bot コマンド説明書

このBotは、あなたのスケジュールを管理し、指定した時間にDiscordで通知を送るリマインダーBotです。

## 利用可能なコマンド一覧

### 1. 予定の登録
**コマンド:** `!timetable`

新しい予定を登録するためのインタラクティブ（対話型）モードを開始します。Botの質問に従って入力してください。

**手順:**
1. `!timetable` と入力します。
2. **タイトル**を入力します（例: `会議`, `燃えるゴミなど`）。
3. **日付**を入力します（例: `2025/12/31`）。
4. **通知タイミング**を入力します。
    - `1d`: 1日前
    - `12h`: 12時間前
    - `0`: 当日（朝9時を基準に計算されます）

---

### 2. 予定の一覧表示
**コマンド:** `!tablelist`

現在登録されているあなたの予定を一覧表示します。
各予定には `1.`, `2.` などの番号が振られており、これは削除時に使用します。

**表示例:**
> 1. 会議 (2025/12/31) - 通知: 1d
> 2. 飲み会 (2026/01/15) - 通知: 3h

---

### 3. 予定の削除
**コマンド:** `!delete <番号>`

指定した番号の予定を削除します。番号は `!tablelist` で確認してください。

**使用例:**
- `!delete 1` : リストの1番目の予定を削除します。

---

### 4. 通知先チャンネルの設定
**コマンド:** `!setch`

リマインド通知を受け取るチャンネルを設定します。
デフォルトではDM（ダイレクトメッセージ）に送られますが、サーバー内の特定のチャンネルに通知したい場合に使用します。

**手順:**
1. `!setch` と入力します。
2. 通知先のチャンネルをメンション形式で入力します（例: `#general`）。

---

### 5. リマインド動作テスト（管理者用）
**コマンド:** `!testremind`

強制的にリマインドチェック処理を実行します。通常は1分ごとに自動実行されますが、すぐに通知テストを行いたい場合に使用します。
